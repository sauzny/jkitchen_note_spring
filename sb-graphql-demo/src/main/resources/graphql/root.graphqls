
schema {
    query: Query
    mutation: Mutation
}

# 查询类型
type Query {
	version: String
}

# 修改类型
type Mutation {
	version: String
}

input Pagination {
    # pageNum starts in 1, the default is 1.
    pageNum: Int
    pageSize: Int!
    # default false
    needTotal: Boolean
}